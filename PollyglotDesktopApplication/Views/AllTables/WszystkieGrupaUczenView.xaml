<views:WszystkieViewBase x:Class="PollyglotDesktopApp.Views.AllTables.WszystkieGrupaUczenView"
                         xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                         xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                         xmlns:views="clr-namespace:PollyglotDesktopApp.Views">

    <DataGrid AutoGenerateColumns="False"
              IsReadOnly="True"
              ItemsSource="{Binding List}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Grupa" Binding="{Binding Grupa}" />
            <DataGridTextColumn Header="Uczeń" Binding="{Binding Uczen}" />
            <DataGridTextColumn Header="Data dołączenia" Binding="{Binding DataDolaczeniaDisplay}" />
            <DataGridTextColumn Header="Data zakończenia" Binding="{Binding DataZakonczeniaDisplay}" />
        </DataGrid.Columns>
    </DataGrid>

</views:WszystkieViewBase>
