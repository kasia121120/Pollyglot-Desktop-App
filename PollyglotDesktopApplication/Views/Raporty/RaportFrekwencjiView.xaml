<UserControl x:Class="PollyglotDesktopApp.Views.Raporty.RaportFrekwencjiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="420"
             MinWidth="700"
             Background="White">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Raport frekwencji"
                   FontSize="26"
                   FontWeight="SemiBold" />

            <StackPanel Grid.Row="1"
                        Orientation="Horizontal"
                        Margin="0,12,0,0"
                        VerticalAlignment="Center">
                <TextBlock Text="Okres (YYYY-MM)"
                           FontWeight="Bold"
                           VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding Okresy}"
                          SelectedItem="{Binding Okres, Mode=TwoWay}"
                          Width="140"
                          Margin="12,0,0,0" />
                <Button Content="Oblicz"
                        Command="{Binding ObliczCommand}"
                        Margin="12,0,0,0"
                        Padding="10,4" />
                <Button Content="Eksport frekwencji (CSV)"
                        Command="{Binding EksportFrekwencjiCommand}"
                        Margin="12,0,0,0"
                        Padding="10,4" />
            </StackPanel>

        <Grid Grid.Row="2"
              Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="#f5f5f5"
                    Padding="10"
                    Margin="0,0,12,0"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Zajęcia zaplanowane"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding ZaplanowaneZajecia}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Background="#f5f5f5"
                    Padding="10"
                    Margin="0,0,12,0"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Obecności"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding Obecnosci}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="#f5f5f5"
                    Padding="10"
                    Margin="0,0,12,0"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Nieobecności"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding Nieobecnosci}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="3"
                    Background="#f5f5f5"
                    Padding="10"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Średnia frekwencja"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding SredniaFrekwencja, StringFormat={}{0:N2}%}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID ucznia"
                                    Binding="{Binding UczenId}"
                                    Width="80" />
                <DataGridTextColumn Header="Imię"
                                    Binding="{Binding Imie}"
                                    Width="140" />
                <DataGridTextColumn Header="Nazwisko"
                                    Binding="{Binding Nazwisko}"
                                    Width="140" />
                <DataGridTextColumn Header="Grupa"
                                    Binding="{Binding Grupa}"
                                    Width="120" />
                <DataGridTextColumn Header="Zajęcia zaplanowane"
                                    Binding="{Binding ZaplanowaneZajecia}"
                                    Width="120" />
                <DataGridTextColumn Header="Obecności"
                                    Binding="{Binding Obecnosci}"
                                    Width="90" />
                <DataGridTextColumn Header="Nieobecności"
                                    Binding="{Binding Nieobecnosci}"
                                    Width="90" />
                <DataGridTextColumn Header="Frekwencja"
                                    Binding="{Binding FrekwencjaProcent, StringFormat={}{0:N2}%}"
                                    Width="110" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
