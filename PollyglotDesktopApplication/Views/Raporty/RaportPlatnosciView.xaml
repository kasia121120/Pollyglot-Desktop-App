<UserControl x:Class="PollyglotDesktopApp.Views.Raporty.RaportPlatnosciView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="400"
             MinWidth="600"
             Background="White">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Raport płatności"
                   FontSize="26"
                   FontWeight="SemiBold" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,12,0,0"
                    VerticalAlignment="Center">
            <TextBlock Text="Okres (YYYY-MM)"
                       VerticalAlignment="Center"
                       FontWeight="Bold" />
            <ComboBox ItemsSource="{Binding Okresy}"
                      SelectedItem="{Binding Okres, Mode=TwoWay}"
                      Width="140"
                      Margin="12,0,0,0" />
            <Button Content="Oblicz"
                    Command="{Binding ObliczCommand}"
                    Margin="12,0,0,0"
                    Padding="10,4" />
            <Button Content="Eksport płatności (CSV)"
                    Command="{Binding EksportPlatnosciCommand}"
                    Margin="12,0,0,0"
                    Padding="10,4" />
        </StackPanel>

        <Grid Grid.Row="2"
              Margin="0,12,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Background="#f5f5f5"
                    Padding="10"
                    Margin="0,0,12,0"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Oczekiwane"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding ExpectedTotal, StringFormat={}{0:N2}}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Background="#f5f5f5"
                    Padding="10"
                    Margin="0,0,12,0"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Wpłacone"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding PaidTotal, StringFormat={}{0:N2}}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Background="#f5f5f5"
                    Padding="10"
                    CornerRadius="4">
                <StackPanel>
                    <TextBlock Text="Zaległość"
                               FontWeight="Bold" />
                    <TextBlock Text="{Binding BalanceTotal, StringFormat={}{0:N2}}"
                               FontSize="18"
                               FontWeight="SemiBold" />
                </StackPanel>
            </Border>
        </Grid>

        <DataGrid Grid.Row="3"
                  ItemsSource="{Binding Rows}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Single"
                  Margin="0,0,0,8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID ucznia"
                                    Binding="{Binding UczenId}"
                                    Width="80" />
                <DataGridTextColumn Header="Imię"
                                    Binding="{Binding Imie}"
                                    Width="120" />
                <DataGridTextColumn Header="Nazwisko"
                                    Binding="{Binding Nazwisko}"
                                    Width="120" />
                <DataGridTextColumn Header="Oczekiwane"
                                    Binding="{Binding ExpectedAmount, StringFormat={}{0:N2}}"
                                    Width="120" />
                <DataGridTextColumn Header="Zapłacone"
                                    Binding="{Binding PaidAmount, StringFormat={}{0:N2}}"
                                    Width="120" />
                <DataGridTextColumn Header="Saldo"
                                    Binding="{Binding Balance, StringFormat={}{0:N2}}"
                                    Width="120" />
                <DataGridTextColumn Header="Status"
                                    Binding="{Binding StatusRaportu}"
                                    Width="120" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
