<local:JedenViewBase x:Class="PollyglotDesktopApp.Views.Add.NewGrupaUczenView"
                    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:PollyglotDesktopApp.Views"
                    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                    mc:Ignorable="d"
                    d:DesignHeight="240"
                    d:DesignWidth="360">

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0"
               Grid.Column="0"
               VerticalAlignment="Center"
               Content="Grupa"/>
        <Label Grid.Row="0"
               Grid.Column="1"
               Background="White"
               Padding="4"
               VerticalAlignment="Center"
               Content="{Binding GrupaDane}"
               HorizontalAlignment="Left"/>
        <Button Grid.Row="0"
                Grid.Column="2"
                Content="..."
                Command="{Binding ShowGrupyCommand}"
                Width="30"
                Height="24"
                Margin="8,0,0,0"/>
        <TextBlock Grid.Row="1"
                   Grid.Column="1"
                   Foreground="Red"
                   FontSize="12"
                   Text="{Binding GrupaError}"
                   Margin="4,0,0,0"/>

        <Label Grid.Row="2"
               Grid.Column="0"
               VerticalAlignment="Center"
               Content="Uczen"/>
        <Label Grid.Row="2"
               Grid.Column="1"
               Background="White"
               Padding="4"
               VerticalAlignment="Center"
               Content="{Binding UczenDane}"
               HorizontalAlignment="Left"/>
        <Button Grid.Row="2"
                Grid.Column="2"
                Content="..."
                Command="{Binding ShowUczniowieCommand}"
                Width="30"
                Height="24"
                Margin="8,0,0,0"/>
        <TextBlock Grid.Row="3"
                   Grid.Column="1"
                   Foreground="Red"
                   FontSize="12"
                   Text="{Binding UczenError}"
                   Margin="4,0,0,0"/>

        <Label Grid.Row="4"
               Grid.Column="0"
               VerticalAlignment="Center"
               Content="Data dolaczenia"/>
        <DatePicker Grid.Row="4"
                    Grid.Column="1"
                    SelectedDate="{Binding DataDolaczenia, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Margin="4,0,0,0"
                    Width="150"
                    HorizontalAlignment="Left"/>

        <Label Grid.Row="5"
               Grid.Column="0"
               VerticalAlignment="Center"
               Content="Data zakonczenia"/>
        <DatePicker Grid.Row="5"
                    Grid.Column="1"
                    SelectedDate="{Binding DataZakonczenia, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                    Margin="4,0,0,0"
                    Width="150"
                    HorizontalAlignment="Left"/>
    </Grid>

</local:JedenViewBase>
