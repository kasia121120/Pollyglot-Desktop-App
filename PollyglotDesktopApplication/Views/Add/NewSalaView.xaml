<views:JedenViewBase x:Class="PollyglotDesktopApp.Views.Add.NewSalaView"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                     xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                     xmlns:views="clr-namespace:PollyglotDesktopApp.Views"
                     mc:Ignorable="d"
                     d:DesignHeight="400"
                     d:DesignWidth="600">

    <Grid Margin="20">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>


        <Label Grid.Row="0" Grid.Column="0" Content="Nazwa:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding Nazwa, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Pojemność:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Pojemnosc, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>


        <Label Grid.Row="2" Grid.Column="0" Content="Lokalizacja:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Lokalizacja, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Wyposażenie:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding Wyposazenie, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Typ:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding Typ, UpdateSourceTrigger=PropertyChanged}" Margin="0,5"/>

    </Grid>

</views:JedenViewBase>
